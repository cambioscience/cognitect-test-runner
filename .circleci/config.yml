version: 2
jobs:
  build:
    docker:
    - image:  clojure:openjdk-11-tools-deps
    steps:
    - checkout
    - run:
        name: run tests
        command: |
          set -e
          clojure -Atest